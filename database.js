var mysql=require('mysql');
var con = mysql.createConnection{{
    host:"localhost",
    user: "root",
    password: ""
    
}};

con.connect(function(err){
    if(err) throw err;
    console.log("Connected!");
    conn.query("CREATE DATABASE Database4",function(err,result);
        
    var sql="CREATE TABLE stud1(name varchar(30),roll int(5),per int("
    
})